# 逮老二服务端第3版更新说明

## 更新内容

- 选牌阶段固定30秒，超时自动选牌。
- 任意玩家出牌后，立即向房间内三名玩家广播选择状态。
- 每轮结算广播三家的完整牌面、牌型、得分变化和文字版支付关系。
- 使用一张公共牌后立即从公共牌数组中移除。
- 三家牌型强弱不同时，由中间名次向最大和最小名次分别支付本轮分值。
- 支持每轮亮牌3秒、玩家确认、摸两张牌、再来一回合和退出房间。

## 上传方式

1. 解压服务端压缩包。
2. 把解压后的全部文件放到 GitHub 仓库 `ziheda/dailaoer` 的根目录，替换同名文件；不要只把 ZIP 文件上传到仓库。
3. 提交到 `main` 分支。
4. 微信云托管绑定该仓库后会触发新的构建和发布。
5. 等“线上版本”显示新的发布时间且状态为“服务正常”后，再测试小游戏。

云托管服务监听端口为 `3000`，Dockerfile 已包含相应配置。浏览器不能访问 `0.0.0.0:3000`；`0.0.0.0` 只是容器内部监听地址。

## 已验证规则

- 第一轮：豹子、对子、散牌 => `+1、-2、+1`。
- 第二轮：豹子、对子、散牌 => `+2、-4、+2`。
- 三名玩家总分始终守恒为0。
