# 逮老二服务端第10版更新说明

## 本版更新

- 随机竞技优先匹配真人，最早排队玩家等待15秒仍未满3人时自动用人机补足空位。
- 2名真人补1名人机，1名真人补2名人机；好友房不加入人机。
- 人机只使用自己的手牌和公开公共牌，第4轮暗牌对人机同样不可见。
- 新增普通、均衡、进阶三档概率策略，人机会评估当前牌型和剩余手牌价值，但保留合理失误概率，不固定输赢。
- 人机自动完成选牌和轮次确认，并使用与真人完全相同的服务端计分及牌豆结算。
- 房间快照、亮牌结果和历史记录新增 `isBot` 标记，客户端会明确展示人机身份。
- 新增人机单元测试和单真人全五轮人机匹配测试；原好友房及三真人随机匹配流程保持不变。

## 上传方式

1. 解压服务端第10版压缩包。
2. 将解压后的全部文件覆盖到 GitHub 仓库 `ziheda/dailaoer` 根目录，不要上传ZIP文件本身。
3. 提交到 `main` 分支，等待微信云托管自动构建和发布。
4. 确认新版本服务正常后，再上传第10版小游戏客户端。

服务端继续监听 `3000` 端口，微信云托管服务名仍为 `dailaoer`。可通过环境变量 `MATCHMAKING_BOT_WAIT_MS` 修改人机补位时间，未配置时默认为15秒。

## 当前限制

人机不会操控牌堆或读取暗牌。牌豆总余额目前仍保存在客户端本机，正式运营前仍应迁移到基于微信身份的服务端钱包和流水数据库。
