# 逮老二服务端第7版更新说明

## 本版更新

- 为每一个五轮大回合生成唯一 `matchId`，并同步 `matchNumber`。
- 客户端通过 `matchId` 保证每个大回合只消耗一次体力、只发放一次牌豆奖励，断线重连不会重复结算。
- 原有房间、计分、30秒自动选牌、第4轮暗牌、记牌器及实时牌堆协议保持不变。
- 不需要数据库，不涉及充值或支付接口。

## 上传方式

1. 解压服务端第7版压缩包。
2. 把解压后的全部文件覆盖到 GitHub 仓库 `ziheda/dailaoer` 根目录，不要上传ZIP压缩包本身。
3. 提交到 `main` 分支，等待微信云托管自动构建和发布。
4. 确认微信云托管显示新的发布时间且服务正常后，再上传第7版小游戏客户端。

服务端仍监听 `3000` 端口，微信云托管服务名仍为 `dailaoer`。
