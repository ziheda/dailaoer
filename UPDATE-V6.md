# 逮老二服务端第6版更新说明

## 本版更新

- 每次向客户端同步牌桌状态时，增加 `deckCount`（剩余牌堆数量）。
- 客户端使用该数量绘制实时扑克堆；原有房间、计分、30秒自动选牌、第4轮暗牌和记牌器协议保持不变。
- 不需要创建数据库，也不需要修改微信云托管的环境ID、服务名或监听端口。

## 上传方式

1. 解压服务端第6版压缩包。
2. 把解压后的全部文件覆盖到 GitHub 仓库 `ziheda/dailaoer` 根目录，不要把ZIP压缩包本身上传到仓库。
3. 提交到 `main` 分支，等待微信云托管自动构建和发布。
4. 确认微信云托管页面显示新的发布时间且服务正常后，再上传第6版小游戏客户端。

服务端仍监听 `3000` 端口，微信云托管服务名仍为 `dailaoer`。
