# 逮老二服务端第8版更新说明

## 本版更新

- 新增随机匹配队列，三名在线玩家进入队列后自动组成随机竞技场并立即开始。
- 房间协议新增 `FRIEND`（好友房）与 `MATCHMAKING`（随机场）两种模式。
- 随机场设置100牌豆参赛门槛，服务端根据五轮最终积分计算零和牌豆结果：总支出与总收入始终相等。
- 第一名获得200牌豆返奖、第二名获得100牌豆返奖、第三名获得0牌豆返奖，对应净变化为 `+100 / 0 / -100`；并列玩家平分对应名次的返奖。
- 随机场中途主动退出按弃赛处理：退出者净扣100牌豆，其余两人各净得50牌豆，并关闭本场匹配。
- 随机场结算带唯一 `matchId`，客户端可避免断线重连或重复消息造成重复记账。
- 原有好友房、五轮计分、30秒自动选牌、第4轮暗牌、记牌器和再来一回合流程保持不变。
- 新增随机匹配端到端测试，同时保留原好友房联机测试。

## 上传方式

1. 解压服务端第8版压缩包。
2. 将解压后的全部文件覆盖到 GitHub 仓库 `ziheda/dailaoer` 根目录，不要上传ZIP压缩包本身。
3. 提交到 `main` 分支，等待微信云托管自动构建和发布。
4. 确认微信云托管显示新的发布时间、运行日志无异常后，再上传第8版小游戏客户端。

服务端仍监听 `3000` 端口，微信云托管服务名仍为 `dailaoer`。

## 当前限制

本版由服务端权威计算每场增减数值，但玩家牌豆总余额仍保存在客户端本机，尚未接入微信身份和云数据库。正式开放随机竞技前，应将钱包余额、结算流水和幂等记录迁移到服务端数据库。
